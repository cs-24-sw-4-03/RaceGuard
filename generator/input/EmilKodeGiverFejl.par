Actor NumActor{
    State{
        double number;
    }
    Knows{
    NumActor testActor;
    }
    Spawn(double x){
        State.number = x;
    }
    on getSquared(){
        double result;
        result = squared(State.number);
    }

    local squared(double num): double{
        return num*num;
    }
}

main(){}